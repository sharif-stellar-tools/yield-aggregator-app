# Rust / Cargo
/target
Cargo.lock

# Soroban test snapshots (generated by `cargo test`)
test_snapshots

# Soroban / Stellar local config
.soroban
.stellar

# Node
node_modules
artifacts
cache

# Environment & secrets (never commit real values)
.env
.env.*
!.env.example

# Deployment artifacts (contract ids are public; keep generated files out)
deployments/*.json
!deployments/*.example.json

# Wasm build outputs
*.wasm

# TypeChain generated types (Hardhat compilation artifacts)
typechain-types
